# requirements.txt
# Core CrewAI and Pydantic dependencies
crewai
pydantic

# Vertex AI/Gemini SDKs (Ensure these match your working versions)
google-cloud-aiplatform
# Add the unified SDK dependency if your final fix required it:
# google-genai 

# Fallback/Wrapper (If you installed it)
litellm
litellm[proxy]

# Web Server Components
# FastAPI and ASGI server (Required by LiteLLM proxy)
fastapi
uvicorn

Flask
gunicorn
python-dotenv
